const fs = require('fs');
let file = fs.readFileSync('src/pages/supervisor/TeamManagement.tsx', 'utf8');
file = file.replace('              <div className="flex gap-3 pt-4">\n                <button \n                  type="button"\n                  onClick={() => setIsEditModalOpen(false)}', `              <div className="space-y-2">\n                <label className="text-[11px] font-bold text-coke-white uppercase tracking-widest pl-1">Vincular à Loja</label>\n                <select \n                  value={editStoreId}\n                  onChange={(e) => setEditStoreId(e.target.value)}\n                  className="w-full bg-coke-darker border border-coke-gray rounded-xl p-4 text-coke-white focus:ring-1 focus:ring-coke-red outline-none appearance-none"\n                >\n                  <option value="">Selecione uma loja (Opcional)</option>\n                  {stores.map(store => (\n                    <option key={store.id} value={store.id}>\n                      {store.name}\n                    </option>\n                  ))}\n                </select>\n              </div>\n\n              <div className="flex gap-3 pt-4">\n                <button \n                  type="button"\n                  onClick={() => setIsEditModalOpen(false)}`);
fs.writeFileSync('src/pages/supervisor/TeamManagement.tsx', file);
